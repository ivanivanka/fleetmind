## Markster FleetMind AI - Example environment file
## Copy to `.env` and fill in as needed. Do NOT commit `.env`.

# Host port to bind the container to (compose maps HOST_PORT -> 8000 in-container)
HOST_PORT=8000

# Simulation tuning
FLEET_SIZE=12
TASK_RATE=3.0
MAX_QUEUED_TASKS=25
MAX_TOTAL_TASKS=2000

# Optional: Gemini
GEMINI_MODEL=gemini-flash-latest
GEMINI_API_KEY=your-key-here

